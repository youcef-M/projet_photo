<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.julien.likrone" >

    <application

        android:allowBackup="true"
        android:icon="@drawable/cosmo"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".ExampleActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".InscriptionActivity" android:label="Inscription" />
        <activity android:name=".LoginActivity" android:label="Example" />
        <activity android:name=".Photo" android:label="Photo" />
        <activity android:name=".AcceuilActivity" android:label="Acceuil" />

        <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    </application>

</manifest>
